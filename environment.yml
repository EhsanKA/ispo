name: ispo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    # Note: numpy and pandas will be installed via uv pip with specific versions
    # numpy==1.26.4
    # pandas==2.2.2
    - scikit-learn>=1.0.0
    - scipy>=1.7.0
    - psutil>=5.8.0
    - GPUtil>=1.4.0
    - memory-profiler>=0.60.0
    - optuna>=3.0.0
    - plotly>=5.0.0
    - wandb>=0.12.0
    - pytest>=7.0.0
    - black>=22.0.0
    - flake8>=4.0.0





